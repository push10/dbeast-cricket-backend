server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dbeast
    username: dbeast_user
    password: dbeast_pass

  jpa:
    hibernate:
      ddl-auto: update # Change from 'create' once the first run works
    show-sql: true